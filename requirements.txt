# Core AI-layer dependencies (see requirements-ai.txt from the instructor —
# merged here so `pip install -r requirements.txt` sets up the whole project).
numpy==2.1.3
pydantic==2.9.2

# Pick the provider(s) your team actually uses; all three are pinned so any
# teammate can switch LLM_PROVIDER / EMBEDDING_PROVIDER without reinstalling.
anthropic==0.39.0
openai==1.54.4
google-genai==0.3.0

# --- SE layer ---------------------------------------------------------

# Configuration
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Async HTTP + scraping + feeds
httpx==0.27.2
feedparser==6.0.11
beautifulsoup4==4.12.3

# Retries
tenacity==9.0.0

# Storage
asyncpg==0.30.0

# Scheduling
apscheduler==3.10.4

# Web UI (bonus: login / signup / topic preferences / digest viewer)
fastapi==0.115.6
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.9
itsdangerous==2.2.0
markdown==3.7